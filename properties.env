# =============================================================================
# properties.env - Environment variables used in Seecog Softwares Website
# =============================================================================
# This file consolidates all environment variables referenced in the codebase.
# Copy this to .env for local development, or set these in your deployment.
#
# USAGE: The app loads this file via dotenv. Ensure dotenv.config({ path: 'properties.env' })
#        is called early (see app.js, config/database.js, config/sequelize.js, scripts/seed.js)
# =============================================================================

# -----------------------------------------------------------------------------
# Database (config/database.js, config/sequelize.js, models, migrate.js)
# -----------------------------------------------------------------------------
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=seecogsoftwares_website
DB_USER=root
DB_PASS=

# -----------------------------------------------------------------------------
# Session (app.js - express-session)
# -----------------------------------------------------------------------------
SESSION_SECRET=change-this-to-a-random-secret-in-production

# -----------------------------------------------------------------------------
# Admin seed user (scripts/seed.js - npm run seed / npm run seed-admin)
# -----------------------------------------------------------------------------
ADMIN_EMAIL=admin@seecogsoftwares.com
ADMIN_PASSWORD=Admin@123

# -----------------------------------------------------------------------------
# Email Settings (config/emailConfig.js, controllers/recruitController.js)
# Used for: Resume PDF emails, contact form, etc.
# NOTE: config/emailConfig.js currently uses service: 'gmail' - host/port ignored.
#       EMAIL_HOST and EMAIL_PORT are kept for future custom SMTP support.
# -----------------------------------------------------------------------------
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465
EMAIL_USER=seecogonline@gmail.com
EMAIL_PASS=stxfccykboxhbjik

# -----------------------------------------------------------------------------
# Server (bin/www)
# -----------------------------------------------------------------------------
PORT=3002

# -----------------------------------------------------------------------------
# Node environment (config/sequelize.js, app.js)
# Values: development | production
# Affects: database config selection, session cookie secure flag
# -----------------------------------------------------------------------------
NODE_ENV=development
