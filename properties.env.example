# =============================================================================
# properties.env.example - Template for environment variables
# =============================================================================
# Copy this file to properties.env and fill in your values.
# properties.env is loaded by the application (see app.js, config/, scripts/).
# =============================================================================

# Database (config/database.js, config/sequelize.js)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=seecogsoftwares_website
DB_USER=root
DB_PASS=your_password

# Session (app.js)
SESSION_SECRET=change-this-to-a-random-secret-in-production

# Credentials vault encryption (utils/credentialsCrypto.js)
# Optional. If not set, derives from SESSION_SECRET. Use 32+ chars for AES-256.
# CREDENTIALS_ENCRYPTION_KEY=your-32-char-secret-key-here

# Admin seed user (scripts/seed.js)
ADMIN_EMAIL=admin@seecogsoftwares.com
ADMIN_PASSWORD=Admin@123

# Email - SMTP (config/emailConfig.js, controllers/recruitController.js)
# NOTE: Currently uses Gmail service; EMAIL_HOST/PORT reserved for custom SMTP
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Server (bin/www)
PORT=3002

# Node environment (config/sequelize.js, app.js)
NODE_ENV=development
