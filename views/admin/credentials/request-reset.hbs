<div class="admin-card">
  <h2><i class='bx bx-envelope'></i> Reset Vault Password</h2>
  <p>Request a password reset link. The link will be sent to the email configured for this vault.</p>

  {{#if flash.success}}<div class="flash-success">{{flash.success}}</div>{{/if}}
  {{#if flash.error}}<div class="flash-error">{{flash.error}}</div>{{/if}}

  <form method="POST" action="/admin/credentials/reset/request" style="max-width: 400px; margin-top: 20px;">
    <button type="submit" class="btn btn-admin">Send Reset Link</button>
    <a href="/admin/credentials" class="btn btn-sm" style="margin-left: 12px;">Back</a>
  </form>
</div>
