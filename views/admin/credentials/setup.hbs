<div class="admin-card">
  <h2><i class='bx bx-key'></i> Set Up Credentials Vault</h2>
  <p>Create your credentials vault with a password. The password will be encrypted and stored securely.</p>

  {{#if flash.success}}<div class="flash-success">{{flash.success}}</div>{{/if}}
  {{#if flash.error}}<div class="flash-error">{{flash.error}}</div>{{/if}}

  <form method="POST" action="/admin/credentials/setup" style="max-width: 450px; margin-top: 20px;">
    <div class="form-group">
      <label for="password">Vault Password</label>
      <input type="password" id="password" name="password" class="form-control" required placeholder="Choose a strong password">
    </div>
    <div class="form-group">
      <label for="reset_email">Reset Email</label>
      <input type="email" id="reset_email" name="reset_email" class="form-control" required placeholder="pankaj.7613@gmail.com" value="pankaj.7613@gmail.com">
      <small style="color: #64748b;">Password reset links will be sent to this email.</small>
    </div>
    <button type="submit" class="btn btn-admin">Create Vault</button>
  </form>
</div>
